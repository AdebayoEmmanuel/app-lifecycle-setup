apiVersion: v1
kind: Service
metadata:
  name: api-gateway-green
  namespace: ecommerce
  labels:
    app: api-gateway
    version: green
spec:
  type: ClusterIP
  selector:
    app: api-gateway
    version: green
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
